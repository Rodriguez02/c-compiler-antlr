
int func(int a, int b){
    if(a > 10 && b < 20){
        return 1;
    }else{
        return 0;
    }
}

int mult(int a, int b){
    return (a * b + 1);
}

int main(){
    int x = 0;
    int y = x;
    int i;
    int x = mult(4,10);
    int sol;

    if(x == 1 && y == 0){
        for(i = 0; i < (x - 2 / 2); i=i+1){
            if(i == 6){
                sol = 10;
            }
        }
        sol = 0;
    }
    int x = 10;
    int result;
    while(x < 20){
        x = x + 1;
        result = x * 2;
    } 

}

si no hay otro ID con el mismo nombre, lo agrego de una
si hay otro ID, y es de tipo Variable, notificar que ya existe ese ID
si hay otro ID, y es de tipo funcion, y esta inicializado (es una declaracion de funcion), notificar que ya existe ese ID
si hay otro ID, y es de tipo funcion, y no esta inicializado (es un prototipo), hacer comparaciones necesarias